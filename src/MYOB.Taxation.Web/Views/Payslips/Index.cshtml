@using MYOB.Taxation.Web.Startup
@model MYOB.Taxation.Web.Models.Payslips.PayslipViewModel
@{
    ViewBag.Title = L("Payslips");
    ViewBag.ActiveMenu = PageNames.Payslips; //Matches with the menu name in TaxationNavigationProvider to highlight the menu item
}
@section scripts
{
    <environment names="Development">
        <script src="~/js/views/payslips/index.js" asp-append-version="true"></script>
    </environment>
    <environment names="Staging,Production">
        <script src="~/js/views/payslips/index.min.js" asp-append-version="true"></script>
    </environment>
}
@*<h2>
    <span><a class="btn btn-primary btn-sm" asp-action="Create" asp-route-id="">@L("CreatePaySlip")</a></span>
</h2>*@
<form id="PayslipCreationForm">

    @*<div class="form-group">
            <label for="Title">@L("Title")</label>
            <input type="text" name="Title" class="form-control" placeholder="@L("Title")" required maxlength="@Acme.SimpleTaskApp.Tasks.Task.MaxTitleLength">
        </div>
        <div class="form-group">
            <label for="Description">@L("Description")</label>
            <input type="text" name="Description" class="form-control" placeholder="@L("Description")" maxlength="@Acme.SimpleTaskApp.Tasks.Task.MaxDescriptionLength">
        </div>*@
    <div class="form-group">
        @Html.Label(L("Employee"))
        @Html.DropDownList(
            "EmployeeId",
            Model.Employees,
            new
            {
                @class = "form-control",
                id = "EmployeeCombobox"
            })
    </div>
    <button type="submit" class="btn btn-default">@L("Save")</button>
    <div id="payslipDiv">

    </div>
</form>

@*<div class="row">
    <div>
        <ul class="list-group" id="PaySlips">
            @foreach (var payslip in Model.Payslips)
            {
                <li class="list-group-item">

                    <h4 class="list-group-item-heading">@payslip.FirstName</h4>
                    <div class="list-group-item-text">
                        @payslip.IncomeTax
                    </div>
                </li>
            }
        </ul>
    </div>
</div>*@